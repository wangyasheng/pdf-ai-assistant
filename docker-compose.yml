version: '3.8'

services:
  app-builder:
    build: .                 # 根据当前目录的 Dockerfile 进行编译
    image: crpi-rqm712svm0emwmme.cn-shanghai.personal.cr.aliyuncs.com/wang-space/pdf-ai-service:latest # 阿里云镜像完整地址
    container_name: pdf-ai-builder-runner
    restart: "no"            # 打包完直接退出，不需要它一直运行
    entrypoint: ["echo", "Build and Tag Done!"]